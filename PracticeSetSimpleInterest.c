#include<stdio.h>

int main(){

    int principal=100, rate=4, years=1;

    int SimpleInterest = (principal*rate*years)/100;

    printf("The valube of simple interest is %d", SimpleInterest);

    return 0;
}